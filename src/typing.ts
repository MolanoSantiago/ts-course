(() => {
  // Inferred type: number
  let price = 100;
// Inferred type: string
  let product = 'Café';

// Explicit type: number
  let priceTyped: number = 200;
// Explicit type: string
  let productTyped: string = 'Café';
})();
